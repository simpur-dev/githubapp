# R12 API 26 原生技术选型说明

> 原则：系统能力优先、不引第三方库；每项给出 API 名、版本、替代方案与收益。
> 兼容基线：compatibleSdkVersion 6.1.0(23) / targetSdkVersion 26.0.0。

## 1. 已采用的原生能力（代码落点）

| 能力 | API / 版本 | 落点 | 替代方案（未采用原因） |
|---|---|---|---|
| 应用主题框架 | `ThemeControl.setDefaultTheme(CustomTheme)` @12（darkColors @20） | common/ui/theme/SystemThemeBridge | 自绘弹窗配色（回归面大、系统组件无法覆盖） |
| 系统材质弹窗 | `systemMaterial` + `ImmersiveMaterial` @26 | CommonModal（AlertDialog/ActionSheet） | 手写毛玻璃（GPU 开销高、深浅色要自己做） |
| HDS 浮动页签 | `HdsTabs` + `barFloatingStyle`（@kit.UIDesignKit） | HomePage | 自绘 TabBar（手势/材质/避让全部要自己维护） |
| 原生侧边栏 | `SideBarContainer` @9 | HomePage 抽屉 | 手写 Stack overlay（R9 前旧实现，已删） |
| 系统符号图标 | `SymbolGlyph` + sys.symbol 资源 | 全应用图标层 | FA 字体图标（R11 实证：渲染丢字） |
| 列表左滑 | `ListItem.swipeAction` @9 | PullLoadMoreList（enableRowSwipe 参数化） | HdsListItem（需整行结构重构，登记待办） |
| 帧率声明 | `expectedFrameRateRange` @11 | SkeletonList/PressableCard/分段切换动画 | displaySync 自绘循环（无场景） |
| Repeat 虚拟滚动 | `Repeat.virtualScroll` + `memoryOptimizationStrategy` @26 | PullLoadMoreList 及全部列表 | LazyForEach + IDataSource（V1 时代方案，样板多） |
| 网页深色 | `Web.darkMode(Auto)` + `forceDarkAccess` @10 | WebPage/LoginWebPage/ReadmeTab/CodeDetailPage | 注入 CSS（维护成本高、第三方页面不兼容） |
| 状态栏跟随 | `setWindowSystemBarProperties` 页面级声明 + onBackground/onForeground 重置 | SafeAreaInsets/NavTitleBridge | 全屏窗口+自绘标题栏（R10 实证：.title/.menus builder 不渲染） |
| 主题切换即时生效 | `ApplicationContext.setColorMode` @11 | EntryAbility.syncAppColorMode | 逐页刷色（原生标题栏不可见的根因，R10 已证） |
| Markdown 解析 | MD4C（C++ NDK 侧 SAX 解析） | feature-repo/cpp + MarkdownNative | Web 渲染（性能差、深色割裂） |
| 数据存储 | Preferences（配置/主题/语言）+ RDB（事件/README/浏览历史缓存） | common/base/dao | 文件自管（无事务、无版本迁移） |
| 网络 | `@ohos.net.http` 封装（超时/重试/错误码映射/限流提示） | common/base/net/HttpManager | 三方网络库（禁令） |

## 2. 显式偏差决策（有据可查）

### 2.1 GraphQL（目标规格 vs 现实）

- **决策：REST 为主，GraphQL 预留不启用。**
- 依据：
  1. GitHub GraphQL 需要 PAT 勾选 GraphQL 权限域且通知/列表接口返回模型差异大，切换是全服务层回归；
  2. 本项目当前瓶颈在渲染层与缓存策略（已解决），不在请求次数——REST 分页接口每页已合并取数；
  3. 限流：REST 认证后 5000 req/h，当前场景余量充足。
- 启用条件（登记）：出现"单页 >3 次串行聚合请求"的场景（如仓库页首屏聚合）再评估。

### 2.2 HdsListItem 左滑 vs 原生 swipeAction

- 决策：本轮用原生 `ListItem.swipeAction`（@9 起稳定、参数化零开销）。
- HdsListItem 官方样式更贴 HDS 规范，但要求整行重构为 HdsListItem 卡片形态，涉及全部列表 id
  契约回归——登记 R13 候选。

### 2.3 五 Tab vs 官方 4 Tab

- 官方 App 实际 4 Tab（Home/Notifications/Explore/Profile，见 GitHub Blog 2026-03 changelog）；
  按目标规格实现 5 Tab，"代码"Tab = 全局搜索一级入口（能力对齐官方搜索，入口化）。

## 3. 未采用能力登记（调研过、暂无场景）

| 能力 | 版本 | 不采用原因 |
|---|---|---|
| `@Env WINDOW_AVOID_AREA` | @26 | 非全屏窗口模型下系统自动避让，无直接消费场景 |
| `NavigationTitleOptions.scrollEffectOptions` | @26 | 非全屏下与原生标题材质叠加效果待逐页实测，登记 R13 |
| `List.backPressBehavior` | @26 | 左滑行当前无打开态持久化需求 |
| 通知推送（Push Kit） | — | GitHub 无官方推送端点，轮询/角标方案需服务端配合，超出本轮 |
| Web 组件 AI Session | @26 | 无翻译/摘要场景 |
